# Core
Django>=5.2,<5.3
gunicorn>=25.3.0
whitenoise>=6.7
dj-database-url>=2.2
psycopg2-binary>=2.9.12

# SMS & webhooks
twilio>=9.0
requests>=2.34.2

# HTML sanitizer for inbound email (parser-based; regex approach was unsafe)
nh3>=0.3.6

# Email (Resend HTTP API — Railway blocks outbound SMTP)
resend>=2.32.2

# OIDC identity provider — Keel as IdP for the DockLabs suite (Phase 2b)
django-oauth-toolkit>=2.4

# Avatar storage (Phase 2 of suite-wide profile, keel >= 0.27.0)
# Pillow processes uploads; django-storages[s3] writes to docklabs-avatars
# bucket via CloudFront. Without these, avatar features fall back to local
# FS — fine for dev but lost on Railway redeploy.
Pillow>=12.2.0
django-storages[s3]>=1.14

# Optional (uncomment as needed)
# django-allauth>=65.0   # SSO via Microsoft Entra ID
# anthropic>=0.40         # AI features
