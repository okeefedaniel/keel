# Core
Django>=5.2,<5.3
gunicorn>=22.0
whitenoise>=6.7
dj-database-url>=2.2
psycopg2-binary>=2.9

# SMS & webhooks
twilio>=9.0
requests>=2.30

# HTML sanitizer for inbound email (parser-based; regex approach was unsafe)
nh3>=0.2

# Email (Resend HTTP API — Railway blocks outbound SMTP)
resend>=2.0

# OIDC identity provider — Keel as IdP for the DockLabs suite (Phase 2b)
django-oauth-toolkit>=2.4

# Optional (uncomment as needed)
# django-allauth>=65.0   # SSO via Microsoft Entra ID
# anthropic>=0.40         # AI features
