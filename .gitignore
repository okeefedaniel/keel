__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/
*.egg
.venv/
venv/
.env
db.sqlite3
*.sqlite3
dev.db
staticfiles/
.gstack/
.DS_Store

# Claude Code runtime state
.claude/*.lock
.claude/

# Nightly test suite artifacts (scripts/nightly.sh, scripts/nightly-qa.sh).
# Both write here on every run. Untracked, they are one `git add -A` away
# from being committed by the suite's own auto-fix phase.
logs/
reports/
